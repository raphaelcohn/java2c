dependency_check_RedHat_checkFunction()
{
	# https://github.com/coto/server-easy-install/blob/master/lib/core.sh
	# Also /etc/SuSE-release, /etc/mandrake-release, /etc/UnitedLinux-release and /etc/debian_version
	if [ -f /etc/redhat-release ]; then
		readonly dependency_distributionName="RedHat"
		return 0
	fi
	return 1
}
dependency_check_register dependency_check_RedHat_checkFunction

dependency_check_RedHat_packageManager()
{
	dependency_check_CentOS_packageManager
}
